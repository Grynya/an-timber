import {createTheme} from "@mui/material/styles";

const theme = createTheme(
    {
        palette: {
            background: {
                default: "#f2eecb"
            },
            primary: {
                main: "#235A11"
            },
            secondary: {
                main: "#FFFFFF"
            }
        }
    }
);
export {theme};
